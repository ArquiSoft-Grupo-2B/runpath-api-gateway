services:
  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    env_file:
      - .env
    command: npm start
    ports:
      - '8888:8888'
      - '9876:9876'
    networks:
      - orchestration_net
      - backend_net
networks:
  orchestration_net:
    external: true
  backend_net:
    external: true
